{
  "rules": {
    "users": {
       "$uid": {
           ".read": "$uid === auth.uid",
           ".write": "$uid === auth.uid"
        }
     },
     "boards": {
        "$boardId": {
            ".read": true,
            ".write": true
         }
     }
  }
}
